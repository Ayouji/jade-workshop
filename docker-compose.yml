services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jade_workshop_app
    restart: always
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
      - PORT=3000
